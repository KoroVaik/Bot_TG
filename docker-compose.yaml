services:
  bot:
    build: .
    image: my_bot
    container_name: bot_debug
    restart: always
    tty: true
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
    logging:
      driver: "json-file"
      options:
        max-size: "10mb"
